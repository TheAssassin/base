/*
 * Private header that can be included by other software. It is wrapped in
 * another public header that contains include guards.
 *
 * The functions defined in this header represent functions written in Python.
 * Calling them results in the Python interpreter running the corresponding
 * Python function.
 */

void pysrvInit();
